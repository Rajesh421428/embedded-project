#include "main.h"
#include "led.h"   // ✅ include your LED driver

int main(void)
{
  HAL_Init();
  SystemClock_Config();

  LED_Init();   // ✅ call your init instead of MX_GPIO_Init()

  while (1)
  {
    LED_Blink();  // ✅ your function now runs here
  }
}
