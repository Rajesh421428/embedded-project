#ifndef LCD_I2C_H
#define LCD_I2C_H

#include "main.h"
#include <stdarg.h>
#include <stdio.h>

// Change if your backpack is 0x3F instead of 0x27
#define LCD_I2C_ADDR   (0x27 << 1)   // HAL expects 8-bit addr (7-bit << 1)

// 16x2 defaults
#define LCD_COLS 16
#define LCD_ROWS 2

void LCD_Init(I2C_HandleTypeDef *hi2c);
void LCD_Clear(void);
void LCD_SetCursor(uint8_t col, uint8_t row);
void LCD_Print(const char *s);
void LCD_Printf(const char *fmt, ...);
void LCD_Backlight(uint8_t on);

#endif
